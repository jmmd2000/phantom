{
  "name": "@jamesmddoyle/phantom",
  "version": "0.1.1",
  "description": "API mocker and traffic inspector with a built-in dashboard.",
  "author": "James Doyle",
  "license": "MIT",
  "bin": {
    "phantom": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "scripts": {
    "build": "tsc && cp package.json ./dist/ && cp -r ../dashboard/build ./dist/dashboard-ui",
    "dev": "node --watch --experimental-strip-types index.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "cac": "^7.0.0"
  }
}
